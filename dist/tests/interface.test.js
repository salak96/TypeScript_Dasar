describe('interface', () => {
    it("should support in typescript", () => {
        const seller = {
            id: 1,
            name: 'TOKO ABC',
            address: 'Jakarta'
        };
        console.log(seller);
    });
});
export {};
