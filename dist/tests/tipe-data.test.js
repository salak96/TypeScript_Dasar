"use strict";
describe('Tipe Data', () => {
    it('should must declare', () => {
        const name = 'Hello Lambang';
        const balance = 100000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
